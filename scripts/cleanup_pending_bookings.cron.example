# Run pending-bookings cleanup every day at 01:30.
# Replace /path/to/instance with your actual buildout instance path.
30 1 * * * /path/to/instance/bin/cleanup_pending_bookings >> /var/log/plone/cleanup_pending_bookings.log 2>&1
